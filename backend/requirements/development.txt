# =============================================================================
# development.txt — Developer tooling, testing, linting, and formatting
# Includes all of base.txt
# =============================================================================
-r base.txt

# --- Testing ---
pytest==9.1.1
pytest-asyncio==1.4.0
anyio[trio]

# --- Code Quality / Linting / Formatting ---
ruff==0.15.20
black==26.5.1
isort==5.13.2

# --- Type Checking ---
mypy_extensions==1.1.0

# --- Build / Packaging Tools (dev only) ---
# These are transitive build-system requirements; not needed at runtime
build==1.5.0
pyproject_hooks==1.2.0
setuptools==81.0.0
wheel==0.47.0
shellingham==1.5.4
typer==0.25.1
